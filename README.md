# RedRover solution README

## Get started

To run tests you will need Node **v22**.

Run `npm i`

`npm run build` to make sure no errors in transit

To run application `npm run solve`, just replace the input with your test params or update the tests with your test cases.

`npm run test` runs the existing tests.
